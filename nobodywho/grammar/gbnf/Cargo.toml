[package]
name = "gbnf"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1"
jsonschema = { version = "0.41.0", default-features = false, features = ["resolve-file"] }

[dev-dependencies]
insta = "1.46.1"
gbnf-macro = { path = "../gbnf-macro" }
